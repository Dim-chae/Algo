#include <bits/stdc++.h>
using namespace std;

int main() {
    int c = 1;
    
    while (1) {
        int N, M;
        cin >> N;
        if (N == 0) break;
        
        while (N--) cin >> M;
        cout << "Case " << c++ << ": Sorting... done!" << '\n';
    }
    
}