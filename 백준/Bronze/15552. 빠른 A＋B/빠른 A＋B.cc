#include <bits/stdc++.h>
#define endl '\n'

using namespace std;

int a, b, t;

int main() {
    ios::sync_with_stdio(0);
    cin.tie(0);

    cin >> t;
    for(int i = 0; i < t; i++){
        cin >> a >> b;
        cout << a + b << endl;
    }
}