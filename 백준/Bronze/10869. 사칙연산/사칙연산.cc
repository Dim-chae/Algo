#include <bits/stdc++.h>

using namespace std;

int main() {
    cin.tie(0);
    ios::sync_with_stdio(0);

    int A, B;
    cin >> A >> B;

    cout << A + B << '\n' << A - B << '\n' << A * B << '\n' << A / B << '\n' << A % B;
}