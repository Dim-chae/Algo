#include <bits/stdc++.h>
using namespace std;

int main() {
    cin.tie(0);
    ios::sync_with_stdio(0);

    int N;
    cin >> N;

    if (N % 2 == 1) cout << "SK";
    else cout << "CY";
}